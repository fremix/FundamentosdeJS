/** Selectores del DOM 
 * Para seleccionar una clase se coloca .nombredelaClase
 * Para seleccionar un ID se usa el numeral #nombredelID
 * Para seleccionar un elemento HTML, solo se le pasa en elemento HTML
*/

const heading = document.querySelector('.heading');

/*
console.log(heading);
console.log(heading.textContent);
console.log(heading.tagName);
console.log(heading.classList);
console.log(heading.id);
*/

const enlaces = document.querySelectorAll('.navegacion a');

console.log(enlaces);






